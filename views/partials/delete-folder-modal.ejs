<div
  class="delete-folder-modal hidden fixed inset-0 bg-black bg-opacity-30 flex justify-center items-center"
>
  <form
    action="/folders/<%= currentFolder.id %>/delete"
    method="POST"
    class="max-w-96 bg-white p-6 rounded-lg flex flex-col"
  >
    <h1 class="text-center font-semibold text-base mb-2">
      Are you sure you want to delete this folder?
    </h1>
    <p class="text-sm">
      This will delete the folder and all files within it. This action cannot be
      undone.
    </p>
    <div class="flex justify-end mt-4">
      <button
        type="button"
        class="cancel bg-stone-200 hover:bg-hover-gray rounded-lg p-2 mr-2 text-sm font-semibold"
      >
        Cancel
      </button>
      <button
        class="bg-red-600 hover:bg-red-500 rounded-lg p-2 text-white text-sm font-semibold"
      >
        Delete
      </button>
    </div>
  </form>
</div>
